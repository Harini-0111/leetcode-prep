# Find The Kth Largest Integer In The Array

[Problem Link](https://leetcode.com/problems/find-the-kth-largest-integer-in-the-array/)

## Details

**Difficulty:** Medium  
**Topics:** Array · String · Divide and Conquer · Sorting · Heap (Priority Queue) · Quickselect  

## Problem

You are given an array of strings nums and an integer k. Each string in nums represents an integer without leading zeros.

Return the string that represents the kth largest integer in nums.

Note: Duplicate numbers should be counted distinctly. For example, if nums is ["1","2","2"], "2" is the first largest integer, "2" is the second-largest integer, and "1" is the third-largest integer.

## Examples

### Example 1

```
Input: nums = ["3","6","7","10"], k = 4
Output: "3"
Explanation:
The numbers in nums sorted in non-decreasing order are ["3","6","7","10"].
The 4th largest integer in nums is "3".
```

### Example 2

```
Input: nums = ["2","21","12","1"], k = 3
Output: "2"
Explanation:
The numbers in nums sorted in non-decreasing order are ["1","2","12","21"].
The 3rd largest integer in nums is "2".
```

### Example 3

```
Input: nums = ["0","0"], k = 2
Output: "0"
Explanation:
The numbers in nums sorted in non-decreasing order are ["0","0"].
The 2nd largest integer in nums is "0".
```

### Example 4

```
solution = Solution()
nums = ["3", "6", "1", "10"]
k = 2
print(solution.kthLargestNumber(nums, k))  # Output: "6"
```

### Example 5

```
import java.util.*;

public class Solution {
    public String kthLargestNumber(String[] nums, int k) {
        // Step 1: Convert strings to integers
        List<Integer> numberList = new ArrayList<>();
        for (String s : nums) {
            numberList.add(Integer.parseInt(s));
        }

        // Step 2: Sort the list in ascending order
        Collections.sort(numberList);

        // Step 3: Get the k-th largest element
        int kthLargest = numberList.get(numberList.size() - k);

        // Step 4: Convert the result to string
        return String.valueOf(kthLargest);
    }

    public static void main(String[] args) {
        Solution sol = new Solution();
        String[] nums = {"3", "6", "1", "10"};
        int k = 2;
        System.out.println(sol.kthLargestNumber(nums, k));  // Output: "6"
    }
}
```

### Example 6

```
#include <iostream>
#include <vector>
#include <string>
#include <algorithm>

using namespace std;

class Solution {
public:
    string kthLargestNumber(vector<string>& nums, int k) {
        // Step 1: Convert strings to integers
        vector<int> numberList;
        for (const string& s : nums) {
            numberList.push_back(stoi(s));
        }

        // Step 2: Sort the list in ascending order
        sort(numberList.begin(), numberList.end());

        // Step 3: Get the k-th largest element
        int kthLargest = numberList[numberList.size() - k];

        // Step 4: Convert result to string and return
        return to_string(kthLargest);
    }
};

int main() {
    Solution sol;
    vector<string> nums = {"3", "6", "1", "10"};
    int k = 2;
    cout << sol.kthLargestNumber(nums, k) << endl;  // Output: "6"
    return 0;
}
```


## Submission

**Language:** python3  
**Runtime:** 14 ms (95.81%)  
**Memory:** 25.1 MB (42.77%)  
**Submitted:** 2026-07-17

## Solution

```python
class Solution:
    def kthLargestNumber(self, nums: List[str], k: int) -> str:
        num_list = [int(num) for num in nums]
        num_list.sort()
        kth_largest = num_list[len(num_list) - k]
        return str(kth_largest)
```

---

*Generated by [DevGrid](https://github.com/yourusername/devgrid)*
